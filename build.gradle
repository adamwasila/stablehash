subprojects {
    group 'org.wasila.stablehash'
    version = "0.0.2-SNAPSHOT"

    project.ext {
        publicationRepoName = "maven"
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}

